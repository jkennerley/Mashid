chai.should();

describe("Mocha Cahi demo", function () {

    it("varous assertions with chai", function () {

        // Arrange

        // Act

        // Assert
        false.should.equal(false); 
        true.should.equal(true); 
        "0".should.equal("0"); 

        //null.should.equal(null);
        //0.should.equal(0); 

    });
});
