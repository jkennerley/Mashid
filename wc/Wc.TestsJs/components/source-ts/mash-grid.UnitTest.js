import test from 'ava'

//import { demo } from './demo-module';

//let adder = function(a, b) {return a + b;};

//test("mash-grid unit test", t => {
//    t.is( adder(1,2), 3,    "");
//});
//

test('can add numbers', t => {
    t.is(1 + 1, 2);
});

test('adder', t => {
    //t.is(adder(1,2), 0);
    //t.is(adder(1,2), 3);
});
